{
  "name": "control-them",
  "version": "0.0.1",
  "description": "Controls if an HTTP request should be allowed to be passed to next set of routers.",
  "main": "index.js",
  "scripts": {
    "start": "rm -rf dist && tsc && node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx ts-node-dev src/index.ts"
  },
  "private": true,
  "author": "anuragvohraec@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "@types/express": "^4.17.9",
    "@types/mocha": "^8.2.0",
    "mocha": "^8.2.1",
    "ts-node-dev": "^1.1.1",
    "tslint": "^6.1.3",
    "typescript": "^4.1.3"
  },
  "dependencies": {
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1"
  }
}
